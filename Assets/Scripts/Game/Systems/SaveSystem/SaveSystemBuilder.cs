using Game.Model;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.Systems.Save
{
    public class SaveSystemBuilder
    {
      /*  public SaveSystem Make(GameData GameData)
        {
            var saveSystem = new SaveSystem(ref GameData);
            return saveSystem;
        }
      */
    }

}
